/**
 * The MEDKONG icon mark, inline SVG. Sized by height; `tone` picks the
 * linework for white (`light`), ink (`dark`) or primary-teal (`onTeal`) grounds.
 * Always paired with the wordmark except in square, constrained placements.
 */
export { MedkongMark } from '@/components/shared/MedkongMark';
