/**
 * App-window chrome for product mockups: title bar on #F7F9F7 with wordmark →
 * divider → tenant → live dot, clock, avatar; 14px radius; window shadow.
 * Build the contents at a fixed width and wrap the whole thing in
 * `<div className="mkscale" data-scale="1240">` so it scales, never reflows.
 */
export { Window } from '@/components/macs/mock';
