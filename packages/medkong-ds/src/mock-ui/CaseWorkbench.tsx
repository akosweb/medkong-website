/**
 * The MAC case workbench — a complete review-board window (case header, KPI
 * strip, active P1 step with PAR elements and machine output, locked rail,
 * evidence pane). Built at 1240px; wrap in `.mkscale` with `data-scale="1240"`.
 * Sample data, fictional beneficiary.
 */
export { CaseWorkbench } from '@/components/macs/CaseWorkbench';
