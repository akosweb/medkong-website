import { TrustBand } from '@medkong/ds';

export const Homepage = () => (
  <TrustBand claim="Enterprise data foundations, workflow orchestration and governed AI operations — the infrastructure layer under every module." />
);
export const ForMacs = () => (
  <TrustBand claim="Ontology-backed case objects, governed actions and full lineage — the infrastructure layer under every review gate." chips={['Human in the loop', 'Audit trail per action', 'Policy-aware findings']} />
);
