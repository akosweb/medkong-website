import { Section, SectionHead } from '@medkong/ds';

export const Gray = () => (
  <Section ground="gray" padding="48px 0">
    <SectionHead eyebrow="End-to-end workflow" headline="Nine gates from receipt to provider letter." support="A case opens automatically on submission. Three intake gates screen it, four medical-review gates judge it, and the decision stage issues the outcome." />
  </Section>
);
export const Mint = () => (
  <Section ground="mint" padding="48px 0">
    <SectionHead eyebrow="Product intelligence" headline="AI that works inside the workflow." />
  </Section>
);
export const Teal = () => (
  <Section ground="teal" padding="48px 0">
    <SectionHead onTeal eyebrow="Why Palantir" headline="MEDKONG is built on Palantir Foundry." />
  </Section>
);
export const Tint = () => (
  <Section ground="tint" padding="48px 0">
    <SectionHead eyebrow="Request a demo" headline="Bring modular AI to the revenue cycle." />
  </Section>
);
